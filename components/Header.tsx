import Link from 'next/link';

export function Header() {
  return (
    <header className="w-full p-6">
      <div className="container mx-auto">
        {/* Header content removed since logo is now in the form */}
      </div>
    </header>
  );
}